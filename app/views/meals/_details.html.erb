<h2> <%= meal.name %> </h2>

<% @meal.portions.each do |portion| %>
  <% if portion.ingredient.present? %>
    <div class="d-flex justify-content-between">
      <p><%= portion.ingredient.name %> | <%= portion.quantity %>g</p>
      <p><%= portion.ingredient.calories %> kcal</p>
    </div>
  <% end %>
<% end %>
